server {
    listen 80;
    server_name {{ domain_name }};

    location / {
        proxy_pass http://localhost:3000; # Points to our Swarm App
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}